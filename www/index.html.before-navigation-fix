<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
<title>G Messenger</title>

<style>
*{box-sizing:border-box}
html,body{margin:0;width:100%;height:100%;font-family:Arial,sans-serif;background:#fff;color:#102033}
button,input,textarea{font:inherit}
button{border:0;cursor:pointer}

.screen{display:none;width:100%;height:100vh}
.screen.active{display:flex}

.login{align-items:center;justify-content:center;padding:25px;background:#f5f9ff}
.card{width:min(430px,100%);background:#fff;border-radius:24px;padding:30px;box-shadow:0 10px 40px #00000015;text-align:center}
.logo{width:78px;height:78px;border-radius:50%;background:#087cff;color:#fff;display:flex;align-items:center;justify-content:center;font-size:38px;font-weight:bold;margin:0 auto 18px}
h1{margin:8px 0;font-size:27px}
.sub{color:#667781;line-height:1.5;margin-bottom:25px}
.field{text-align:left;margin:15px 0}
.field label{display:block;font-weight:bold;font-size:13px;margin-bottom:7px}
.field input{width:100%;padding:14px;border:1px solid #d8e1eb;border-radius:12px;outline:none}
.field input:focus{border-color:#087cff}
.primary{width:100%;padding:14px;border-radius:12px;background:#087cff;color:#fff;font-weight:bold;margin-top:8px}
.secondary{width:100%;padding:13px;border-radius:12px;background:#eef5ff;color:#087cff;margin-top:10px}

.app{flex-direction:column;background:#fff}
.header{background:#087cff;color:white;padding:18px 18px 14px}
.header-row{display:flex;justify-content:space-between;align-items:center}
.title{font-size:23px;font-weight:bold}
.icon{background:transparent;color:#fff;font-size:25px}
.search{margin-top:15px;width:100%;padding:13px 16px;border:0;border-radius:14px;outline:none}
.filters{display:flex;gap:8px;padding:10px 14px;border-bottom:1px solid #edf0f3}
.filter{padding:8px 13px;border-radius:20px;background:#f0f4f8;color:#52606d}
.filter.active{background:#087cff;color:#fff}
.chat-list{flex:1;overflow-y:auto;padding-bottom:75px}


/* Modern G Messenger navigation */
.bottom{
  height:70px;
  display:flex;
  align-items:stretch;
  justify-content:space-around;
  background:#fff;
  border-top:1px solid #e5eaf0;
  position:relative;
  z-index:50;
}

.bottom button{
  flex:1;
  border:0;
  background:transparent;
  color:#687784;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:4px;
  font-size:11px;
  font-weight:600;
  cursor:pointer;
}

.bottom button span{
  font-size:21px;
  line-height:1;
}

.bottom button.active{
  color:#087cff;
}

.bottom button.active span{
  transform:scale(1.05);
}

.chat-list{
  padding-bottom:12px;
}

.chat{
  min-height:72px;
  display:flex;
  align-items:center;
  gap:12px;
  padding:11px 16px;
  background:#fff;
  border-bottom:1px solid #eef1f4;
}

.chat:active{
  background:#f2f7ff;
}

.chat-info{
  min-width:0;
  flex:1;
}

.chat-name{
  font-size:16px;
  font-weight:700;
  color:#102033;
}

.chat-preview{
  margin-top:4px;
  font-size:13px;
  color:#71808d;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

.chat-time{
  align-self:flex-start;
  font-size:11px;
  color:#84919c;
  margin-top:4px;
}

.chat-unread{
  min-width:20px;
  height:20px;
  padding:0 6px;
  border-radius:10px;
  display:flex;
  align-items:center;
  justify-content:center;
  background:#087cff;
  color:#fff;
  font-size:11px;
  font-weight:bold;
}

.new-chat-button{
  z-index:60;
}

.contacts-body{
  background:#f5f8fb;
}

.contact-row{
  min-height:70px;
}

.contact-row .avatar{
  flex:0 0 auto;
}

.contacts-search{
  box-sizing:border-box;
}

@media(max-width:480px){
  .contacts-header{
    padding:14px;
  }

  .contacts-title{
    font-size:20px;
  }
}

.contacts-screen{flex-direction:column;background:#f5f8fb}

.contacts-header{
  background:#087cff;
  color:white;
  padding:18px;
}

.contacts-header-row{
  display:flex;
  align-items:center;
  gap:12px;
}

.contacts-back{
  background:none;
  color:white;
  font-size:28px;
}

.contacts-title{
  font-size:22px;
  font-weight:bold;
  flex:1;
}

.contacts-search{
  margin-top:14px;
  width:100%;
  padding:13px 16px;
  border:0;
  border-radius:14px;
  outline:none;
  background:white;
  color:#102033;
}

.contacts-body{
  flex:1;
  overflow-y:auto;
}

.contact-row{
  display:flex;
  align-items:center;
  gap:13px;
  padding:14px 17px;
  background:white;
  border-bottom:1px solid #eef1f4;
  cursor:pointer;
}

.contact-row:active{
  background:#f0f6ff;
}

.contact-info{
  flex:1;
  min-width:0;
}

.contact-name{
  font-weight:bold;
  font-size:16px;
}

.contact-username{
  color:#667781;
  font-size:13px;
  margin-top:4px;
}

.contact-phone{
  color:#8696a0;
  font-size:12px;
  margin-top:3px;
}

.contact-arrow{
  color:#087cff;
  font-size:22px;
}

.contacts-empty{
  text-align:center;
  color:#8696a0;
  padding:50px 25px;
  line-height:1.6;
}

.new-chat-button{
  position:fixed;
  right:18px;
  bottom:82px;
  width:56px;
  height:56px;
  border-radius:50%;
  background:#087cff;
  color:white;
  font-size:27px;
  box-shadow:0 6px 20px #00000025;
}

.chat{display:flex;align-items:center;padding:15px 17px;border-bottom:1px solid #eef1f4;gap:13px}
.chat:active{background:#f4f8fc}
.avatar{width:50px;height:50px;border-radius:50%;background:#087cff;color:#fff;display:flex;align-items:center;justify-content:center;font-weight:bold;font-size:20px;flex:none}
.chat-info{flex:1;min-width:0}
.chat-name{font-weight:bold;font-size:16px}
.chat-preview{color:#667781;font-size:13px;margin-top:5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.chat-right{font-size:12px;color:#8696a0}
.gemma{background:linear-gradient(135deg,#087cff,#005bea);color:white}
.gemma .chat-preview{color:#e8f2ff}
.empty{text-align:center;color:#8696a0;padding:45px 25px;line-height:1.6}

.bottom{position:fixed;bottom:0;left:0;right:0;height:66px;background:#fff;border-top:1px solid #e4e9ee;display:flex;justify-content:space-around;align-items:center}
.bottom button{background:none;color:#667781;font-size:12px}
.bottom button span{display:block;font-size:21px;margin-bottom:3px}
.bottom .active{color:#087cff}

.chat-screen{flex-direction:column;background:#f5f8fb}
.chat-header{height:65px;background:#087cff;color:white;display:flex;align-items:center;padding:8px 12px;gap:12px}
.back{background:none;color:white;font-size:25px}
.chat-contact{flex:1}
.chat-contact-name{font-weight:bold}
.chat-contact-status{font-size:12px;opacity:.85}
.messages{flex:1;overflow-y:auto;padding:15px}

.message-status{font-size:11px;margin-left:7px;opacity:.8}
.message-status{
  display:inline-block;
  font-size:11px;
  margin-left:7px;
  opacity:.85;
  white-space:nowrap;
  vertical-align:bottom;
}

.message-text{
  white-space:pre-wrap;
}

.edited-label{
  font-size:10px;
  opacity:.7;
  margin-left:6px;
}

.deleted-message{
  opacity:.7;
  font-style:italic;
}

.message-actions{
  position:absolute;
  right:8px;
  bottom:calc(100% + 6px);
  z-index:100;
  display:flex;
  gap:6px;
  background:#fff;
  border:1px solid #dce5ef;
  border-radius:12px;
  padding:6px;
  box-shadow:0 8px 24px #00000020;
}

.message-actions button{
  background:#eef5ff;
  color:#087cff;
  padding:8px 12px;
  border-radius:8px;
  font-size:12px;
  font-weight:bold;
}

.message-actions button.delete{
  background:#fff1f1;
  color:#c62828;
}

.msg.mine{
  position:relative;
}

.msg.theirs{
  position:relative;
}

.message-actions-backdrop{
  position:fixed;
  inset:0;
  z-index:90;
  background:transparent;
}

.message-text{white-space:pre-wrap}
.edited-label{font-size:10px;opacity:.7;margin-left:6px}
.deleted-message{opacity:.75;font-style:italic}
.message-actions{
position:absolute;
z-index:30;
display:flex;
gap:6px;
background:white;
border:1px solid #dce5ef;
border-radius:12px;
padding:6px;
box-shadow:0 6px 20px #00000020;
}
.message-actions button{
background:#eef5ff;
color:#087cff;
padding:7px 10px;
border-radius:8px;
font-size:12px;
}
.message-actions button.delete{
background:#fff1f1;
color:#c62828;
}
.msg{max-width:78%;padding:10px 13px;border-radius:14px;margin:7px 0;line-height:1.4;word-wrap:break-word}
.msg.mine{margin-left:auto;background:#087cff;color:white;border-bottom-right-radius:4px}
.msg.theirs{margin-right:auto;background:white;border-bottom-left-radius:4px}
.composer{display:flex;gap:8px;padding:9px;background:white;border-top:1px solid #ddd}
.composer textarea{flex:1;resize:none;border:1px solid #dce3ea;border-radius:20px;padding:11px 14px;outline:none;max-height:110px}
.send{width:45px;height:45px;border-radius:50%;background:#087cff;color:white}

.gemma-screen{flex-direction:column;background:#f5f8fb}
.gemma-header{background:#087cff;color:#fff;height:65px;display:flex;align-items:center;padding:10px 13px;gap:12px}
.gemma-body{flex:1;overflow:auto;padding:16px}
.gemma-welcome{background:#fff;border-radius:18px;padding:20px;text-align:center}
.gemma-composer{display:flex;gap:8px;padding:9px;background:#fff}
.gemma-composer textarea{flex:1;border:1px solid #ddd;border-radius:20px;padding:11px;resize:none}

/* ===== G MESSENGER 2026 DESIGN ===== */
.app{
  background:#f7f9fc;
}

.app-header{
  background:#087cff !important;
  color:#fff;
  padding:14px 16px 12px !important;
  box-shadow:0 2px 10px #00000018;
}

.app-header h1,
.app-header .title{
  color:#fff;
}

.search{
  background:#fff !important;
  border:0 !important;
  border-radius:14px !important;
  box-shadow:0 2px 8px #00000012;
  padding:13px 16px !important;
}

.filters{
  background:#fff;
  padding:10px 12px !important;
  gap:8px !important;
  border-bottom:1px solid #e8edf3;
}

.filter{
  border:1px solid #dce5ef !important;
  border-radius:20px !important;
  padding:8px 14px !important;
  background:#fff !important;
  color:#526170 !important;
  font-weight:600;
}

.filter.active{
  background:#087cff !important;
  color:#fff !important;
  border-color:#087cff !important;
}

.chat-list{
  background:#f7f9fc;
  padding-bottom:90px !important;
}

.chat{
  background:#fff !important;
  margin:1px 8px !important;
  padding:13px 12px !important;
  border-bottom:1px solid #eef2f6 !important;
  border-radius:12px;
  transition:.15s;
}

.chat:active{
  background:#eef6ff !important;
  transform:scale(.99);
}

.chat-info{
  min-width:0;
}

.chat-name{
  font-size:16px !important;
  font-weight:700 !important;
  color:#102033 !important;
}

.chat-preview{
  color:#718096 !important;
  margin-top:4px;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

.chat-time{
  color:#8795a5 !important;
  font-size:11px;
}

.chat.unread .chat-name{
  font-weight:800;
}

.avatar{
  flex-shrink:0;
  box-shadow:0 1px 4px #00000012;
}

.bottom{
  position:fixed !important;
  left:0;
  right:0;
  bottom:0;
  height:70px !important;
  background:rgba(255,255,255,.97) !important;
  border-top:1px solid #e4eaf1 !important;
  box-shadow:0 -4px 18px #00000012;
  z-index:50;
  display:flex !important;
  justify-content:space-around;
  align-items:center;
  padding-bottom:env(safe-area-inset-bottom);
}

.bottom button{
  flex:1;
  height:62px;
  border:0;
  background:transparent;
  color:#718096;
  font-size:11px;
  font-weight:600;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:4px;
}

.bottom button span{
  font-size:22px;
  line-height:1;
}

.bottom button.active{
  color:#087cff;
}

.new-chat-button{
  right:18px !important;
  bottom:84px !important;
  width:58px !important;
  height:58px !important;
  border:0;
  box-shadow:0 7px 22px #087cff45 !important;
  z-index:60;
}

.contacts-screen{
  background:#f7f9fc !important;
}

.contacts-header{
  background:#087cff !important;
  padding:14px 16px 18px !important;
  box-shadow:0 2px 10px #00000018;
}

.contacts-back{
  width:42px;
  height:42px;
  border:0;
  border-radius:50%;
  color:#fff;
}

.contacts-title{
  font-size:21px !important;
}

.contacts-search{
  border-radius:14px !important;
  box-shadow:0 2px 8px #00000012;
}

.contacts-body{
  padding:8px 0 90px;
}

.contact-row{
  margin:2px 8px;
  border-radius:12px;
  border-bottom:0 !important;
  transition:.15s;
}

.contact-row:active{
  background:#eef6ff !important;
}

.contact-name{
  color:#102033;
}

.contact-username{
  color:#687789;
}

.contact-arrow{
  font-weight:bold;
}

.chat-screen{
  background:#edf3f8 !important;
}

.chat-header{
  background:#087cff !important;
  color:#fff !important;
  box-shadow:0 2px 10px #00000018;
  z-index:20;
}

.chat-contact-name{
  color:#fff !important;
  font-weight:700;
}

.messages{
  background:#edf3f8 !important;
}

.msg{
  max-width:78% !important;
  padding:9px 11px !important;
  border-radius:15px !important;
  box-shadow:0 1px 2px #00000012;
}

.msg.mine{
  background:#087cff !important;
  color:#fff !important;
  border-bottom-right-radius:4px !important;
}

.msg.theirs{
  background:#fff !important;
  color:#17212b !important;
  border-bottom-left-radius:4px !important;
}

.composer{
  background:#fff !important;
  border-top:1px solid #dfe6ed !important;
  padding:9px !important;
}

.composer textarea{
  background:#f3f6f9;
  border:0 !important;
  border-radius:22px !important;
}

.send{
  background:#087cff !important;
  box-shadow:0 3px 10px #087cff35;
}

.gemma-screen{
  background:#edf3f8 !important;
}

.gemma-header{
  background:#087cff !important;
  box-shadow:0 2px 10px #00000018;
}

.gemma-welcome{
  box-shadow:0 2px 10px #00000010;
}

/* cleaner empty states */
.empty,
.contacts-empty{
  color:#7a8998;
}

/* mobile spacing */
@media(max-width:600px){
  .chat{
    margin:1px 5px !important;
  }

  .chat-list{
    padding-bottom:88px !important;
  }
}


/* ===== G MESSENGER MODERN 2026 DESIGN ===== */

body{
  margin:0;
  background:#f5f7fa;
  color:#17212b;
  font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;
}

.app{
  background:#f5f7fa;
}

.app header{
  background:#087cff;
  color:white;
  padding:18px 16px 12px;
}

.app header h2{
  margin:0 0 14px;
  font-size:25px;
  font-weight:750;
  letter-spacing:-.3px;
}

.search{
  width:100%;
  box-sizing:border-box;
  height:46px;
  border:0;
  outline:none;
  border-radius:24px;
  padding:0 18px 0 44px;
  background:white;
  color:#17212b;
  font-size:15px;
  box-shadow:0 2px 8px #00000012;
}

.search::placeholder{
  color:#7b8794;
}

.filters{
  display:flex;
  gap:8px;
  padding:12px 14px;
  overflow-x:auto;
  background:#fff;
  border-bottom:1px solid #e8edf2;
}

.filters::-webkit-scrollbar{
  display:none;
}

.filter{
  border:1px solid #dbe5ef;
  background:#fff;
  color:#526170;
  border-radius:20px;
  padding:8px 15px;
  font-size:13px;
  white-space:nowrap;
}

.filter.active{
  background:#087cff;
  color:#fff;
  border-color:#087cff;
}

.chat-list{
  flex:1;
  overflow-y:auto;
  padding:0 0 100px;
  background:#f5f7fa;
}

.chat{
  display:flex;
  align-items:center;
  gap:13px;
  min-height:74px;
  padding:9px 16px;
  background:#fff;
  border-bottom:1px solid #edf1f5;
  transition:background .15s ease;
}

.chat:active{
  background:#eef6ff;
}

.chat .avatar{
  width:52px;
  height:52px;
  min-width:52px;
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:20px;
  font-weight:700;
  background:#087cff;
  color:#fff;
}

.chat-info{
  flex:1;
  min-width:0;
}

.chat-name{
  font-size:16px;
  font-weight:650;
  color:#17212b;
  display:flex;
  align-items:center;
  gap:6px;
}

.chat-name small{
  font-size:10px;
  background:#e8f2ff;
  color:#087cff;
  padding:3px 6px;
  border-radius:8px;
}

.chat-preview{
  margin-top:4px;
  font-size:13px;
  color:#778493;
  white-space:nowrap;
  overflow:hidden;
  text-overflow:ellipsis;
}

.chat-time{
  align-self:flex-start;
  margin-top:7px;
  font-size:11px;
  color:#8a97a5;
}

.unread-badge{
  min-width:20px;
  height:20px;
  padding:0 6px;
  border-radius:11px;
  background:#087cff;
  color:white;
  font-size:11px;
  display:flex;
  align-items:center;
  justify-content:center;
  font-weight:700;
}

.empty{
  margin:40px 20px;
  padding:25px;
  background:white;
  border-radius:18px;
  text-align:center;
  color:#7c8895;
  box-shadow:0 2px 12px #00000008;
}

.new-chat-button{
  position:fixed;
  right:20px;
  bottom:82px;
  width:58px;
  height:58px;
  border:0;
  border-radius:50%;
  background:#087cff;
  color:white;
  font-size:29px;
  line-height:58px;
  box-shadow:0 7px 22px #087cff55;
  z-index:40;
}

.bottom{
  position:fixed;
  left:0;
  right:0;
  bottom:0;
  height:68px;
  display:flex;
  align-items:stretch;
  justify-content:space-around;
  background:#fff;
  border-top:1px solid #e5eaf0;
  z-index:50;
  padding-bottom:env(safe-area-inset-bottom);
}

.bottom button{
  flex:1;
  border:0;
  background:transparent;
  color:#758291;
  font-size:11px;
  font-weight:600;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:4px;
}

.bottom button span{
  font-size:21px;
  line-height:22px;
}

.bottom button.active{
  color:#087cff;
}

.contacts-screen{
  background:#f5f7fa;
}

.contacts-header{
  background:#087cff;
  padding:12px 16px 16px;
}

.contacts-title{
  font-size:20px;
  font-weight:750;
}

.contacts-search{
  height:46px;
  box-sizing:border-box;
  margin-top:13px;
  border-radius:24px;
  padding:0 17px;
  font-size:15px;
  border:0;
  outline:none;
}

.contact-row{
  min-height:72px;
  padding:9px 16px;
  border-bottom:1px solid #edf1f5;
}

.contact-row .avatar{
  width:50px;
  height:50px;
  border-radius:50%;
  background:#087cff;
  color:#fff;
  display:flex;
  align-items:center;
  justify-content:center;
  font-weight:700;
  font-size:19px;
}

.chat-screen{
  background:#edf3f8;
}

.chat-header{
  background:#087cff;
  color:#fff;
  min-height:62px;
  display:flex;
  align-items:center;
  gap:10px;
  padding:7px 10px;
}

.chat-contact-name{
  font-size:16px;
  font-weight:700;
}

.composer{
  background:#fff;
  padding:8px;
  border-top:1px solid #dfe6ed;
}

.composer textarea{
  background:#f1f4f7;
  border:0;
}

.msg{
  box-shadow:0 1px 2px #00000012;
}

.msg.mine{
  background:#087cff;
}

.msg.theirs{
  background:#fff;
}

.message-actions{
  border-radius:14px;
}

.gemma-screen{
  background:#f5f7fa;
}

.gemma-header{
  background:#087cff;
}


/* =========================================================
   G MESSENGER — BLUE/WHITE MODERN MESSENGER UI
   ========================================================= */

:root{
  --gm-blue:#087cff;
  --gm-blue-dark:#066bd9;
  --gm-bg:#f4f7fa;
  --gm-border:#e3e9ef;
  --gm-text:#17212b;
  --gm-muted:#718096;
  --gm-white:#ffffff;
}

/* APP */
#appScreen{
  background:var(--gm-bg);
  color:var(--gm-text);
}

/* TOP BAR */
#appScreen > header{
  background:var(--gm-blue) !important;
  color:#fff !important;
  padding:14px 16px 13px !important;
  box-shadow:0 2px 10px #00000018;
}

#appScreen > header h1,
#appScreen > header h2{
  color:#fff !important;
  margin:0;
  font-size:23px;
  font-weight:750;
}

/* SEARCH */
#appScreen .search{
  display:block;
  width:100%;
  box-sizing:border-box;
  height:46px;
  margin-top:12px;
  border:0 !important;
  border-radius:24px !important;
  background:#fff !important;
  color:var(--gm-text);
  outline:none;
  padding:0 17px 0 43px !important;
  font-size:15px;
  box-shadow:0 2px 8px #00000015;
}

#appScreen .search::placeholder{
  color:#7c8997;
}

/* FILTER CHIPS */
#appScreen .filters{
  background:#fff;
  padding:10px 12px;
  border-bottom:1px solid var(--gm-border);
  display:flex;
  gap:8px;
  overflow-x:auto;
}

#appScreen .filters::-webkit-scrollbar{
  display:none;
}

#appScreen .filter{
  flex:none;
  border:1px solid #dbe4ed !important;
  background:#fff !important;
  color:#586777 !important;
  border-radius:18px !important;
  padding:7px 14px !important;
  font-size:13px;
  font-weight:600;
}

#appScreen .filter.active{
  background:var(--gm-blue) !important;
  color:#fff !important;
  border-color:var(--gm-blue) !important;
}

/* CHAT LIST */
#chatList{
  background:var(--gm-bg);
  padding:4px 0 90px !important;
}

#chatList .chat{
  display:flex;
  align-items:center;
  gap:13px;
  min-height:70px;
  padding:9px 16px !important;
  margin:0 !important;
  border-bottom:1px solid #e9eef3 !important;
  background:#fff !important;
  cursor:pointer;
}

#chatList .chat:active{
  background:#edf6ff !important;
}

#chatList .avatar{
  width:52px;
  height:52px;
  min-width:52px;
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  font-weight:700;
  font-size:19px;
}

#chatList .chat-info{
  flex:1;
  min-width:0;
}

#chatList .chat-name{
  color:var(--gm-text) !important;
  font-size:16px;
  font-weight:700;
}

#chatList .chat-preview{
  color:var(--gm-muted) !important;
  font-size:13px;
  margin-top:4px;
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}

#chatList .chat-time{
  color:#8996a3;
  font-size:11px;
}

/* GEMMA */
#chatList .chat.gemma{
  border-bottom:1px solid #e2eaf2 !important;
}

#chatList .chat.gemma .avatar{
  background:#fff !important;
  color:var(--gm-blue) !important;
  border:2px solid #d9eaff;
}

/* EMPTY STATE */
#emptyChats{
  margin:25px 18px;
  padding:28px 20px;
  background:#fff;
  border-radius:18px;
  color:#7b8997;
  text-align:center;
  box-shadow:0 2px 10px #00000008;
}

/* BOTTOM NAVIGATION */
#appScreen .bottom{
  position:fixed !important;
  left:0;
  right:0;
  bottom:0;
  height:68px !important;
  display:flex !important;
  background:rgba(255,255,255,.98) !important;
  border-top:1px solid var(--gm-border) !important;
  box-shadow:0 -3px 15px #00000012;
  z-index:100;
}

#appScreen .bottom button{
  flex:1;
  border:0;
  background:transparent;
  color:#758291;
  font-size:11px;
  font-weight:650;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  gap:4px;
}

#appScreen .bottom button span{
  font-size:21px;
  line-height:21px;
}

#appScreen .bottom button.active{
  color:var(--gm-blue);
}

/* NEW CHAT */
#newChatButton{
  position:fixed !important;
  right:18px !important;
  bottom:82px !important;
  width:58px !important;
  height:58px !important;
  border:0 !important;
  border-radius:50% !important;
  background:var(--gm-blue) !important;
  color:#fff !important;
  font-size:29px !important;
  box-shadow:0 7px 22px #087cff45 !important;
  z-index:110;
}

/* CONTACTS */
#contactsScreen{
  background:var(--gm-bg);
}

.contacts-header{
  background:var(--gm-blue) !important;
  color:#fff;
  padding:10px 14px 16px !important;
  box-shadow:0 2px 10px #00000018;
}

.contacts-header-row{
  min-height:44px;
}

.contacts-back{
  border:0;
  background:transparent;
  color:#fff;
  font-size:30px;
}

.contacts-title{
  font-size:21px;
  font-weight:750;
}

.contacts-search{
  width:100%;
  box-sizing:border-box;
  height:46px;
  margin-top:11px;
  border:0 !important;
  border-radius:24px !important;
  padding:0 17px !important;
  outline:none;
  font-size:15px;
}

.contacts-body{
  background:var(--gm-bg);
  padding:5px 0 85px;
}

.contact-row{
  min-height:70px;
  margin:0 !important;
  background:#fff !important;
  border-bottom:1px solid #e9eef3 !important;
}

.contact-row .avatar{
  width:50px;
  height:50px;
  border-radius:50%;
  background:var(--gm-blue);
  color:#fff;
}

/* CHAT HEADER */
#chatScreen .chat-header{
  background:var(--gm-blue) !important;
  color:#fff !important;
  min-height:62px;
  box-shadow:0 2px 9px #00000018;
}

#chatScreen .chat-contact-name{
  color:#fff !important;
  font-weight:700;
}

/* MESSAGES */
#chatScreen{
  background:#edf3f8 !important;
}

#chatScreen #messages{
  background:#edf3f8 !important;
}

#chatScreen .msg{
  box-shadow:0 1px 2px #00000012;
  border-radius:15px !important;
}

#chatScreen .msg.mine{
  background:var(--gm-blue) !important;
  color:#fff !important;
  border-bottom-right-radius:4px !important;
}

#chatScreen .msg.theirs{
  background:#fff !important;
  color:var(--gm-text) !important;
  border-bottom-left-radius:4px !important;
}

/* COMPOSER */
#chatScreen .composer{
  background:#fff !important;
  border-top:1px solid var(--gm-border) !important;
  padding:8px !important;
}

#chatScreen .composer textarea{
  background:#f1f4f7 !important;
  border:0 !important;
  border-radius:22px !important;
}

#chatScreen .send{
  background:var(--gm-blue) !important;
}

/* GEMMA */
#gemmaScreen{
  background:#edf3f8 !important;
}

.gemma-header{
  background:var(--gm-blue) !important;
  color:#fff !important;
}

/* SMALL MOBILE DEVICES */
@media(max-width:420px){
  #appScreen > header{
    padding-left:13px !important;
    padding-right:13px !important;
  }

  #chatList .chat{
    padding-left:12px !important;
    padding-right:12px !important;
  }

  #chatList .avatar{
    width:50px;
    height:50px;
    min-width:50px;
  }
}


/* ===== PHONE-FRIENDLY G MESSENGER ===== */

*{
  box-sizing:border-box;
  -webkit-tap-highlight-color:transparent;
}

html,body{
  width:100%;
  height:100%;
  margin:0;
  padding:0;
  overflow:hidden;
}

body{
  min-width:0;
  max-width:100vw;
  font-family:Arial,Helvetica,sans-serif;
  background:#f5f8fb;
  overscroll-behavior:none;
}

button,
input,
textarea{
  font:inherit;
}

button{
  touch-action:manipulation;
  cursor:pointer;
}

.screen{
  width:100%;
  height:100dvh;
  min-height:100dvh;
  max-width:100vw;
  overflow:hidden;
}

.app{
  padding-bottom:0 !important;
}

.app header{
  padding-top:max(12px, env(safe-area-inset-top));
}

.chat-list,
.contacts-body,
.messages,
.gemma-body{
  -webkit-overflow-scrolling:touch;
  overscroll-behavior:contain;
}

.chat{
  min-height:68px;
  padding:12px 14px;
}

.chat .avatar,
.contact-row .avatar{
  flex:none;
}

.chat-info{
  min-width:0;
  overflow:hidden;
}

.chat-name,
.chat-preview{
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}

.search{
  width:100%;
  max-width:100%;
  min-height:44px;
  font-size:16px;
}

.filters{
  overflow-x:auto;
  scrollbar-width:none;
  -webkit-overflow-scrolling:touch;
}

.filters::-webkit-scrollbar{
  display:none;
}

.filters button{
  min-height:40px;
  flex:none;
}

.bottom{
  position:fixed !important;
  left:0;
  right:0;
  bottom:0;
  width:100%;
  height:auto !important;
  min-height:64px;
  padding:
    6px 4px
    max(6px, env(safe-area-inset-bottom));
  display:flex;
  z-index:1000;
  background:#fff;
  border-top:1px solid #e1e7ec;
}

.bottom button{
  flex:1;
  min-width:0;
  min-height:52px;
  padding:5px 2px;
  font-size:12px;
}

.bottom button span{
  display:block;
  font-size:21px;
  line-height:25px;
  margin-bottom:2px;
}

.new-chat-button{
  right:16px !important;
  bottom:calc(78px + env(safe-area-inset-bottom)) !important;
  width:54px !important;
  height:54px !important;
  min-width:54px;
  min-height:54px;
  z-index:1100;
}

.contacts-screen{
  width:100%;
  height:100dvh;
}

.contacts-header{
  padding:
    max(12px, env(safe-area-inset-top))
    14px
    14px;
  flex:none;
}

.contacts-header-row{
  min-height:48px;
}

.contacts-back{
  width:44px;
  height:44px;
  min-width:44px;
  padding:0;
}

.contacts-title{
  font-size:20px;
}

.contacts-search{
  min-height:46px;
  font-size:16px;
  margin-top:10px;
}

.contact-row{
  min-height:70px;
  padding:12px 14px;
}

.contact-info{
  min-width:0;
}

.contact-name,
.contact-username,
.contact-phone{
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}

.contact-arrow{
  flex:none;
  width:30px;
  text-align:center;
}

.chat-screen{
  width:100%;
  height:100dvh;
}

.chat-header{
  padding-top:max(6px, env(safe-area-inset-top));
  min-height:60px;
}

.chat-header .back{
  width:44px;
  height:44px;
  min-width:44px;
}

.chat-contact{
  min-width:0;
  flex:1;
}

.chat-contact-name{
  overflow:hidden;
  text-overflow:ellipsis;
  white-space:nowrap;
}

.composer{
  padding:
    7px
    8px
    max(7px, env(safe-area-inset-bottom));
  min-height:60px;
}

.composer textarea{
  min-height:44px;
  max-height:120px;
  font-size:16px;
}

.send{
  width:44px !important;
  height:44px !important;
  min-width:44px;
  min-height:44px;
  flex:none;
}

.msg{
  max-width:84%;
  font-size:15px;
}

.gemma-header{
  padding-top:max(8px, env(safe-area-inset-top));
}

.gemma-composer{
  padding:
    7px
    8px
    max(7px, env(safe-area-inset-bottom));
}

.gemma-composer textarea{
  min-height:44px;
  font-size:16px;
}

@media (max-width:380px){
  .chat{
    padding-left:11px;
    padding-right:11px;
  }

  .chat-name{
    font-size:15px;
  }

  .chat-preview{
    font-size:12px;
  }

  .bottom button{
    font-size:11px;
  }

  .bottom button span{
    font-size:19px;
  }

  .new-chat-button{
    right:12px !important;
  }
}

@media (min-width:600px){
  body{
    background:#e9eef3;
  }

  .screen{
    max-width:600px;
    margin:0 auto;
    background:#f5f8fb;
  }

  .bottom{
    max-width:600px;
    left:50%;
    right:auto;
    transform:translateX(-50%);
  }

  .new-chat-button{
    right:calc(50% - 284px) !important;
  }
}



/* ================================================
   G MESSENGER — PHONE FRIENDLY LAYOUT
   ================================================ */

* {
  box-sizing: border-box;
  -webkit-tap-highlight-color: transparent;
}

html,
body {
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  overflow: hidden;
}

body {
  font-family: Arial, sans-serif;
  background: #f5f8fb;
}

.screen {
  width: 100%;
  height: 100dvh;
  min-height: 100dvh;
  overflow: hidden;
}

button,
input,
textarea {
  font: inherit;
}

button {
  touch-action: manipulation;
}

.app {
  position: relative;
}

.app header {
  flex-shrink: 0;
}

.chat-list {
  flex: 1;
  min-height: 0;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  padding-bottom: 85px;
}

.chat {
  min-height: 72px;
  padding: 12px 15px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.avatar {
  flex-shrink: 0;
}

.chat-info {
  min-width: 0;
  flex: 1;
}

.chat-name,
.contact-name,
.chat-contact-name {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.chat-preview {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.bottom {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  height: 70px;
  padding-bottom: env(safe-area-inset-bottom);
  z-index: 50;
}

.bottom button {
  min-width: 0;
  flex: 1;
  height: 70px;
  border: 0;
  background: transparent;
}

.new-chat-button {
  right: 16px;
  bottom: calc(78px + env(safe-area-inset-bottom));
  z-index: 60;
}

.contacts-screen,
.chat-screen,
.gemma-screen {
  height: 100dvh;
  min-height: 100dvh;
}

.contacts-header {
  flex-shrink: 0;
  padding:
    calc(12px + env(safe-area-inset-top))
    15px
    14px;
}

.contacts-body {
  min-height: 0;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
  padding-bottom: env(safe-area-inset-bottom);
}

.contact-row {
  min-height: 74px;
  padding: 13px 15px;
}

.contacts-search {
  width: 100%;
  min-height: 46px;
  font-size: 16px;
}

.chat-header,
.gemma-header {
  flex-shrink: 0;
  padding-top: calc(8px + env(safe-area-inset-top));
}

.messages,
.gemma-body {
  min-height: 0;
  overflow-y: auto;
  -webkit-overflow-scrolling: touch;
}

.composer,
.gemma-composer {
  flex-shrink: 0;
  padding-bottom: calc(9px + env(safe-area-inset-bottom));
}

.composer textarea,
.gemma-composer textarea {
  min-width: 0;
  font-size: 16px;
}

.send {
  flex-shrink: 0;
}

@media (max-width: 480px) {
  .search {
    font-size: 16px;
    min-height: 44px;
  }

  .filters {
    overflow-x: auto;
    white-space: nowrap;
    -webkit-overflow-scrolling: touch;
  }

  .filters button {
    flex-shrink: 0;
  }

  .chat {
    min-height: 68px;
    padding-left: 13px;
    padding-right: 13px;
  }

  .chat-name {
    font-size: 16px;
  }

  .chat-preview {
    font-size: 13px;
  }

  .contacts-title {
    font-size: 21px;
  }

  .contact-row {
    min-height: 70px;
  }

  .msg {
    max-width: 84%;
  }
}

@media (max-width: 360px) {
  .chat {
    gap: 9px;
    padding-left: 10px;
    padding-right: 10px;
  }

  .avatar {
    flex-shrink: 0;
  }

  .msg {
    max-width: 88%;
  }

  .new-chat-button {
    right: 13px;
  }
}

</style>
</head>

<body>

<!-- LOGIN -->
<section id="loginScreen" class="screen login">
<div class="card">
<div class="logo">G</div>
<h1>G Messenger</h1>
<div class="sub">Fast, private messaging and communication.</div>

<div class="field">
<label>Phone number</label>
<input id="phone" type="tel" placeholder="+234 800 000 0000">
</div>

<button class="primary" onclick="register()">Continue</button>
</div>
</section>

<!-- VERIFY -->
<section id="verificationScreen" class="screen login">
<div class="card">
<div class="logo">G</div>
<h1>Verify your number</h1>
<div class="sub">Enter the six-digit verification code.</div>

<div class="field">
<label>Verification code</label>
<input id="verificationCode" inputmode="numeric" maxlength="6" placeholder="123456">
</div>

<button class="primary" onclick="verify()">Verify</button>
<button class="secondary" onclick="show('loginScreen')">Change number</button>
</div>
</section>

<!-- PROFILE -->
<section id="profileScreen" class="screen login">
<div class="card">
<div class="logo">G</div>
<h1>Create your profile</h1>
<div class="sub">Choose the name and username people will see.</div>

<div class="field">
<label>Your name</label>
<input id="profileName" placeholder="Your name">
</div>

<div class="field">
<label>Username</label>
<input id="username" placeholder="@username">
</div>

<button class="primary" onclick="saveProfile()">Continue</button>
</div>
</section>

<!-- SECURITY -->
<section id="securityScreen" class="screen login">
<div class="card">
<div class="logo">G</div>
<h1>Two-Step Verification</h1>
<div class="sub">Create a six-digit security PIN.</div>

<div class="field">
<label>Security PIN</label>
<input id="securityPin" type="password" inputmode="numeric" maxlength="6" placeholder="123456">
</div>

<div class="field">
<label>Recovery email (optional)</label>
<input id="recoveryEmail" type="email" placeholder="you@example.com">
</div>

<button class="primary" onclick="finishSetup()">Finish setup</button>
</div>
</section>

<!-- HOME -->
<section id="appScreen" class="screen app">

<header class="header">
<div class="header-row">
<div class="title">G Messenger</div>
<button class="icon" onclick="logout()">⋮</button>
</div>
<input class="search" id="search" placeholder="Search G Messenger" oninput="filterChats()">
</header>

<div class="filters">
<button class="filter active">All</button>
<button class="filter">Unread</button>
<button class="filter">Groups</button>
<button class="filter">Favorites</button>
</div>

<main id="chatList" class="chat-list">

<div class="chat gemma" data-name="Gemma" onclick="openGemma()">
<div class="avatar" style="background:white;color:#087cff">G</div>
<div class="chat-info">
<div class="chat-name">Gemma <small>AI</small></div>
<div class="chat-preview">Your G Messenger AI assistant</div>
</div>
<div>›</div>
</div>

<div id="emptyChats" class="empty">
No conversations yet.<br>
Search for a registered user to start chatting.
</div>

</main>

<nav class="bottom">
<button id="navChats" class="active" onclick="goChats()">
<span>💬</span>Chats
</button>
<button id="navUpdates" onclick="goUpdates()">
<span>◉</span>Updates
</button>
<button id="navCommunities" onclick="goCommunities()">
<span>👥</span>Communities
</button>
<button id="navCalls" onclick="goCalls()">
<span>📞</span>Calls
</button>
</nav>
</section>


<button id="newChatButton" class="new-chat-button"
onclick="openContacts()" aria-label="New chat">＋</button>


<!-- CONTACTS -->
<section id="contactsScreen" class="screen contacts-screen">

<header class="contacts-header">
  <div class="contacts-header-row">
    <button class="contacts-back" onclick="goChats()">‹</button>
    <div class="contacts-title">New chat</div>
  </div>

  <input
    id="contactSearch"
    class="contacts-search"
    type="search"
    placeholder="Search contacts"
    autocomplete="off"
    oninput="searchContacts()">
</header>

<main id="contactsList" class="contacts-body">
  <div class="contacts-empty">
    Search for a registered G Messenger user.
  </div>
</main>

</section>

<!-- CHAT -->
<section id="chatScreen" class="screen chat-screen">

<header class="chat-header">
<button class="back" onclick="goChats()">‹</button>
<div class="avatar" id="contactAvatar" style="width:42px;height:42px;background:white;color:#087cff">G</div>
<div class="chat-contact">
<div class="chat-contact-name" id="contactName">Chat</div>
<div class="chat-contact-status" id="contactStatus">online</div>
</div>
</header>

<main id="messages" class="messages"></main>

<div class="composer">
<textarea id="messageInput" rows="1" placeholder="Message" onkeydown="messageKey(event)" oninput="messageTyping()"></textarea>
<button class="send" onclick="sendMessage()">➤</button>
</div>
</section>

<!-- GEMMA -->
<section id="gemmaScreen" class="screen gemma-screen">

<header class="gemma-header">
<button class="back" onclick="show('appScreen')">‹</button>
<div class="avatar" style="width:42px;height:42px;background:white;color:#087cff">G</div>
<div class="chat-contact">
<div class="chat-contact-name">Gemma</div>
<div class="chat-contact-status">G Messenger AI</div>
</div>
</header>

<main class="gemma-body">
<div class="gemma-welcome">
<h2>Welcome to Gemma</h2>
<p>Your G Messenger AI assistant is ready.</p>
<p>Ask questions, get explanations, translate text, or get help with everyday tasks.</p>
</div>
</main>

<div class="gemma-composer">
<textarea id="gemmaInput" rows="1" placeholder="Message Gemma"></textarea>
<button class="send" onclick="sendGemma()">➤</button>
</div>
</section>



<script type="module">
import { Share } from '@capacitor/share';
window.GMShare = Share;
</script>
<script type="module">
import { Contacts } from '@capacitor-community/contacts';
window.GMContacts = Contacts;
</script>
<script src="/socket.io/socket.io.js"></script>
<script>
let token=localStorage.getItem("gmessengerToken")||"";

let socket = null;

function connectRealtime(){
  if(!token) return;

  if(socket){
    try { socket.disconnect(); } catch(e){}
  }

  socket = io();

  socket.on('connect', () => {
    socket.emit('gm:authenticate', token);
  });

  socket.on('gm:user-online', ({userId}) => {
    if(currentConversation &&
       Number(currentConversation.user?.id) === Number(userId)){
      document.getElementById('contactStatus').textContent = 'online';
    }
  });

  socket.on('gm:user-offline', ({userId}) => {
    if(currentConversation &&
       Number(currentConversation.user?.id) === Number(userId)){
      document.getElementById('contactStatus').textContent = 'offline';
    }
  });

  socket.on('gm:typing', ({conversationId, userId, typing}) => {
    if(!currentConversation) return;
    if(Number(currentConversation.id) !== Number(conversationId)) return;
    if(Number(currentConversation.user?.id) !== Number(userId)) return;

    document.getElementById('contactStatus').textContent =
      typing ? 'typing…' : 'online';
  });

  socket.on('gm:message-status', ({conversationId, messageId, status}) => {
    if(!currentConversation) return;
    if(Number(currentConversation.id) !== Number(conversationId)) return;

    const el = document.querySelector('[data-message-id="' + messageId + '"]');
    if(el){
      const statusEl = el.querySelector('.message-status');
      if(statusEl) statusEl.textContent = status === 'delivered' ? '✓✓' : '✓';
    }
  });

  socket.on('gm:message-read', ({conversationId, messageIds}) => {
    if(!currentConversation) return;
    if(Number(currentConversation.id) !== Number(conversationId)) return;

    (messageIds || []).forEach(id => {
      const el = document.querySelector('[data-message-id="' + id + '"]');
      if(el){
        const statusEl = el.querySelector('.message-status');
        if(statusEl) statusEl.textContent = '✓✓';
      }
    });
  });


  socket.on('gm:message-edited', ({conversationId, message}) => {
    if(!currentConversation) return;
    if(Number(currentConversation.id) !== Number(conversationId)) return;

    const el = document.querySelector(
      '[data-message-id="' + message.id + '"]'
    );

    if(!el) {
      loadMessages(conversationId);
      return;
    }

    const text = el.querySelector('.message-text');

    if(text) {
      text.textContent = message.text;
    }

    el.classList.remove('deleted-message');

    const edited = el.querySelector('.edited-label');

    if(!edited) {
      const label = document.createElement('span');
      label.className = 'edited-label';
      label.textContent = ' edited';
      el.appendChild(label);
    }
  });

  socket.on('gm:message-deleted', ({conversationId, messageId}) => {
    if(!currentConversation) return;
    if(Number(currentConversation.id) !== Number(conversationId)) return;

    const el = document.querySelector(
      '[data-message-id="' + messageId + '"]'
    );

    if(el) {
      const text = el.querySelector('.message-text');

      if(text) {
        text.textContent = 'Message deleted';
      }

      el.classList.add('deleted-message');

      const edited = el.querySelector('.edited-label');
      if(edited) edited.remove();

      const status = el.querySelector('.message-status');

      if(status) {
        status.remove();
      }
    }
  });

  socket.on('gm:new-message', ({conversationId, message}) => {
    if(!message) return;

    if(currentConversation &&
       Number(currentConversation.id) === Number(conversationId)) {

      const box = document.getElementById('messages');

      const div = document.createElement('div');
const mine =
  Number(message.sender_id) === Number(currentUser?.id);

div.className = 'msg ' + (mine ? 'mine' : 'theirs');
div.dataset.messageId = message.id;

const text = document.createElement('span');
text.textContent = message.text;
div.appendChild(text);

if(mine){
  const status = document.createElement('span');
  status.className = 'message-status';
  status.textContent =
    message.status === 'delivered' ? '✓✓' : '✓';
  div.appendChild(status);
}

box.appendChild(div);
      setupMessagePress(div);
      box.scrollTop = box.scrollHeight;

    } else {
      loadConversations();
    }
  });
}

let currentPhone='';
let currentConversation=null;
let currentUser=null;

function show(id){
document.querySelectorAll('.screen').forEach(x=>x.classList.remove('active'));
const el=document.getElementById(id);
if(el)el.classList.add('active');
}

async function api(url,options={}){
options.headers=Object.assign({
'Content-Type':'application/json',
'Authorization':'Bearer '+token
},options.headers||{});

const r=await fetch(url,options);
const data=await r.json().catch(()=>({}));

if(!r.ok)throw new Error(data.error||'Request failed');
return data;
}

function startup(){
if(token){
connectRealtime();
setBottomNav('navChats');
show('appScreen');
loadConversations();
}else{
show('loginScreen');
}
}

async function register(){
currentPhone=document.getElementById('phone').value.trim();

if(currentPhone.length<7){
alert('Enter a valid phone number.');
return;
}

try{
await api('/api/auth/register',{
method:'POST',
headers:{Authorization:''},
body:JSON.stringify({phone:currentPhone})
});
show('verificationScreen');
}catch(e){
alert(e.message);
}
}

async function verify(){
const code=document.getElementById('verificationCode').value.trim();

if(!/^[0-9]{6}$/.test(code)){
alert('Enter the 6-digit verification code.');
return;
}

try{
const data=await api('/api/auth/verify',{
method:'POST',
headers:{Authorization:''},
body:JSON.stringify({phone:currentPhone,code})
});

token=data.token;
localStorage.setItem('gmessengerToken',token);
connectRealtime();

currentUser=data.user;

if(data.user.profileComplete){
localStorage.setItem('gmessengerName',data.user.name||'');
localStorage.setItem('gmessengerSetup','complete');
show('appScreen');
await loadConversations();
}else{
show('profileScreen');
}
}catch(e){
alert(e.message);
}
}

async function saveProfile(){
const name=document.getElementById('profileName').value.trim();
const username=document.getElementById('username').value.trim();

if(name.length<2){
alert('Please enter your name.');
return;
}

if(username.length<3){
alert('Please choose a username.');
return;
}

try{
await api('/api/profile',{
method:'POST',
body:JSON.stringify({name,username})
});

show('securityScreen');
}catch(e){
alert(e.message);
}
}

async function finishSetup(){
const pin=document.getElementById('securityPin').value.trim();
const recoveryEmail=document.getElementById('recoveryEmail').value.trim();

if(!/^[0-9]{6}$/.test(pin)){
alert('Create a 6-digit security PIN.');
return;
}

try{
await api('/api/security/two-step',{
method:'POST',
body:JSON.stringify({pin,recoveryEmail})
});

localStorage.setItem('gmessengerSetup','complete');
localStorage.setItem('gmessengerName',document.getElementById('profileName').value.trim());

show('appScreen');
await loadConversations();
}catch(e){
alert(e.message);
}
}

async function loadConversations(){
if(!token)return;

try{
const data=await api('/api/conversations');
const list=document.getElementById('chatList');

list.querySelectorAll('.real-chat').forEach(x=>x.remove());

const empty=document.getElementById('emptyChats');

if(!data.conversations.length){
empty.style.display='block';
return;
}

empty.style.display='none';

data.conversations.forEach(c=>{
if(!c.user)return;

const row=document.createElement('div');
row.className='chat real-chat';
row.dataset.name=c.user.name||c.user.username||c.user.phone||'User';

const name=c.user.name||c.user.username||c.user.phone||'User';
const initial=name.charAt(0).toUpperCase();
const preview=c.lastMessage?.text||'No messages yet';
const time=c.lastMessage?.created_at?
new Date(c.lastMessage.created_at).toLocaleTimeString([],{
hour:'2-digit',minute:'2-digit'
}):'';

row.innerHTML=
'<div class="avatar">'+escapeHtml(initial)+'</div>'+
'<div class="chat-info">'+
'<div class="chat-name">'+escapeHtml(name)+'</div>'+
'<div class="chat-preview">'+escapeHtml(preview)+'</div>'+
'</div>'+
'<div class="chat-right">'+escapeHtml(time)+'</div>';

row.onclick=()=>openConversation(c);
list.insertBefore(row,empty);
});
}catch(e){
console.warn('Conversation refresh:',e.message);
}
}



function setBottomNav(active){
  ['navChats','navUpdates','navCommunities','navCalls']
    .forEach(id=>{
      const el=document.getElementById(id);
      if(el)el.classList.remove('active');
    });

  const activeEl=document.getElementById(active);
  if(activeEl)activeEl.classList.add('active');
}

function goChats(){
  setBottomNav('navChats');
  show('appScreen');
  loadConversations();
}

function goUpdates(){
  setBottomNav('navUpdates');
  alert('Updates is coming next.');
}

function goCommunities(){
  setBottomNav('navCommunities');
  alert('Communities is coming next.');
}

function goCalls(){
  setBottomNav('navCalls');
  alert('Calls is coming next.');
}


async function openContacts(){
  show('contactsScreen');

  const input=document.getElementById('contactSearch');
  const list=document.getElementById('contactsList');

  input.value='';
  list.innerHTML=
    '<div class="contacts-empty">Loading your contacts...</div>';

  try{
    // Browser fallback: use G Messenger registered users.
    // The native Contacts plugin is only available inside the Android app.
    if(!window.GMContacts){
      list.innerHTML =
        '<div class="contacts-empty">Loading G Messenger contacts...</div>';

      try{
        const data=await api('/api/users/search?q=a');
        const users=(data.users || []).filter(u =>
          Number(u.id)!==Number(currentUser?.id)
        );

        if(!users.length){
          list.innerHTML =
            '<div class="contacts-empty">'+
            'No registered G Messenger contacts found.<br><br>'+
            'Use the search box to find someone by name, username, or phone.'+
            '</div>';
          return;
        }

        window.gmDeviceContacts=users.map(u=>({
          id:u.id,
          name:u.name || u.username || u.phone || 'G Messenger User',
          username:u.username || '',
          phones:u.phone ? [u.phone] : [],
          registeredUser:u
        }));

        await renderDeviceContacts();
        return;
      }catch(e){
        throw e;
      }
    }

    const permission =
      await window.GMContacts.requestPermissions();

    if(permission.contacts !== 'granted'){
      list.innerHTML=
        '<div class="contacts-empty">'+
        'G Messenger needs Contacts permission to show your phone contacts.'+
        '<br><br>'+
        '<button onclick="openContacts()" class="primary">Allow Contacts</button>'+
        '</div>';
      return;
    }

    const result =
      await window.GMContacts.getContacts({
        projection:{
          name:true,
          phones:true,
          organizationName:true
        }
      });

    window.gmDeviceContacts =
      (result.contacts || []).map(c=>{
        const name =
          c.name?.display ||
          [c.name?.given,c.name?.middle,c.name?.family]
            .filter(Boolean).join(' ') ||
          'Unknown contact';

        const phones =
          (c.phones || [])
            .map(p=>p.number)
            .filter(Boolean);

        return {
          id:c.contactId || '',
          name:name.trim(),
          phones:phones
        };
      }).filter(c=>c.name && c.name !== 'Unknown contact');

    await renderDeviceContacts();

  }catch(e){
    console.error('Device contacts:',e);

    list.innerHTML=
      '<div class="contacts-empty">'+
      'Could not load phone contacts.<br><br>'+
      escapeHtml(e.message || 'Unknown error')+
      '</div>';
  }
}

async function renderDeviceContacts(query=''){
  const list=document.getElementById('contactsList');

  const q=String(query || '').trim().toLowerCase();

  let contacts=window.gmDeviceContacts || [];

  if(q){
    contacts=contacts.filter(c=>{
      return c.name.toLowerCase().includes(q) ||
        c.phones.some(phone =>
          String(phone).toLowerCase().includes(q)
        );
    });
  }

  if(!contacts.length){
    list.innerHTML=
      '<div class="contacts-empty">No contacts found.</div>';
    return;
  }

  list.innerHTML=
    '<div class="contacts-empty" style="padding:12px">'+
    contacts.length+' contact'+(contacts.length===1?'':'s')+
    '</div>';

  for(const contact of contacts){
    const row=document.createElement('div');
    row.className='contact-row';

    const initial =
      contact.name.charAt(0).toUpperCase();

    const phone =
      contact.phones[0] || '';

    row.innerHTML=
      '<div class="avatar">'+
        escapeHtml(initial)+
      '</div>'+
      '<div class="contact-info">'+
        '<div class="contact-name">'+
          escapeHtml(contact.name)+
        '</div>'+
        (phone
          ? '<div class="contact-phone">'+
              escapeHtml(phone)+
            '</div>'
          : '')+
        '<div class="contact-username" data-contact-status>'+
          'Checking G Messenger…'+
        '</div>'+
      '</div>';

    list.appendChild(row);

    if(phone){
      checkContactRegistration(contact,row);
    }else{
      markContactInvite(row,contact);
    }
  }
}

async function checkContactRegistration(contact,row){
  try{
    const phone=contact.phones[0];

    const data=await api(
      '/api/users/search?q='+
      encodeURIComponent(phone)
    );

    const users=data.users || [];

    const normalized=phone.replace(/\D/g,'');

    const user=users.find(u=>{
      const uphone=String(u.phone || '')
        .replace(/\D/g,'');

      return uphone === normalized ||
        uphone.endsWith(normalized) ||
        normalized.endsWith(uphone);
    });

    if(user){
      row.dataset.registered='true';

      const status=
        row.querySelector('[data-contact-status]');

      if(status){
        status.textContent='● On G Messenger';
        status.style.color='#087cff';
        status.style.fontWeight='700';
      }

      row.onclick=()=>startConversation(user);

    }else{
      markContactInvite(row,contact);
    }

  }catch(e){
    console.warn(
      'Contact registration check:',
      contact.name,
      e.message
    );

    markContactInvite(row,contact);
  }
}

function markContactInvite(row,contact){
  row.dataset.registered='false';

  const status=
    row.querySelector('[data-contact-status]');

  if(status){
    status.textContent='Invite to G Messenger';
    status.style.color='#718096';
  }

  row.onclick=()=>inviteContact(contact);
}

async function inviteContact(contact){
  if(!contact)return;

  const phone=contact.phones?.[0] || '';

  const message=
    'Join me on G Messenger — a fast and private messaging app.';

  // Android/Capacitor
  if(window.GMShare){
    try{
      await window.GMShare.share({
        title:'Invite to G Messenger',
        text:message+(phone ? '\n\nContact: '+phone : ''),
        dialogTitle:'Invite to G Messenger'
      });
      return;
    }catch(e){
      if(e?.name==='AbortError')return;
      console.warn('Native share unavailable:',e);
    }
  }

  // Normal browser
  if(navigator.share){
    try{
      await navigator.share({
        title:'Invite to G Messenger',
        text:message+(phone ? '\n\nContact: '+phone : '')
      });
      return;
    }catch(e){
      if(e?.name==='AbortError')return;
      console.warn('Browser share cancelled/failed:',e);
    }
  }

  // Browser fallback
  const shareText=message+(phone ? '\n\nContact: '+phone : '');

  if(navigator.clipboard){
    try{
      await navigator.clipboard.writeText(shareText);
      alert('Invitation copied. You can paste it into any messaging app.');
      return;
    }catch(e){}
  }

  prompt('Copy this invitation:',shareText);
}

let contactSearchTimer=null;


async function searchContacts(){
  clearTimeout(contactSearchTimer);

  const input=document.getElementById('contactSearch');
  const q=input.value.trim();
  const list=document.getElementById('contactsList');

  if(q.length<2){
    if(window.GMContacts){
      renderDeviceContacts(q);
    }else{
      list.innerHTML=
        '<div class="contacts-empty">'+
        'Type at least 2 characters to search.'+
        '</div>';
    }
    return;
  }

  // Native Android: filter already-loaded phone contacts.
  if(window.GMContacts){
    contactSearchTimer=setTimeout(()=>{
      renderDeviceContacts(q);
    },100);
    return;
  }

  // Browser: search registered G Messenger users.
  list.innerHTML=
    '<div class="contacts-empty">Searching G Messenger...</div>';

  contactSearchTimer=setTimeout(async()=>{
    try{
      const data=await api(
        '/api/users/search?q='+encodeURIComponent(q)
      );

      const users=(data.users || []).filter(u =>
        Number(u.id)!==Number(currentUser?.id)
      );

      if(!users.length){
        list.innerHTML=
          '<div class="contacts-empty">'+
          'No registered users found.'+
          '</div>';
        return;
      }

      list.innerHTML='';

      users.forEach(user=>{
        const row=document.createElement('div');
        row.className='contact-row';

        const name=
          user.name ||
          user.username ||
          user.phone ||
          'G Messenger User';

        const initial=name.charAt(0).toUpperCase();

        row.innerHTML=
          '<div class="avatar">'+
          escapeHtml(initial)+
          '</div>'+
          '<div class="contact-info">'+
            '<div class="contact-name">'+
              escapeHtml(name)+
            '</div>'+
            (user.username
              ? '<div class="contact-username">'+
                escapeHtml('@'+user.username)+
                '</div>'
              : '')+
            (user.phone
              ? '<div class="contact-phone">'+
                escapeHtml(user.phone)+
                '</div>'
              : '')+
            '<div class="contact-username" style="color:#087cff;font-weight:700">'+
              '● On G Messenger'+
            '</div>'+
          '</div>'+
          '<div class="contact-arrow">›</div>';

        row.onclick=()=>startConversation(user);
        list.appendChild(row);
      });

    }catch(e){
      console.error('Browser contact search:',e);
      list.innerHTML=
        '<div class="contacts-empty">'+
        escapeHtml(e.message || 'Search failed.')+
        '</div>';
    }
  },250);
}async function startConversation(user){
  if(!user || !user.id)return;

  try{
    show('chatScreen');

    const name =
      user.name ||
      user.username ||
      user.phone ||
      'User';

    const avatar =
      document.getElementById('contactAvatar');

    const contactName =
      document.getElementById('contactName');

    if(contactName){
      contactName.textContent = name;
    }

    if(avatar){
      avatar.textContent =
        name.charAt(0).toUpperCase();
    }

    const data=await api('/api/conversations',{
      method:'POST',
      body:JSON.stringify({
        userId:Number(user.id)
      })
    });

    const conversation=data.conversation;

    conversation.user=user;

    currentConversation=conversation;

    document.getElementById('contactName').textContent=
      user.name ||
      user.username ||
      user.phone ||
      'User';

    document.getElementById('contactAvatar').textContent=
      (
        user.name ||
        user.username ||
        user.phone ||
        'U'
      ).charAt(0).toUpperCase();

    show('chatScreen');

    await loadMessages(conversation.id);
    await loadConversations();

  }catch(e){
    alert(e.message);
  }
}

function openConversation(c){
currentConversation=c;
document.getElementById('contactName').textContent=
c.user.name||c.user.username||c.user.phone||'User';

document.getElementById('contactAvatar').textContent=
(c.user.name||c.user.username||'U').charAt(0).toUpperCase();

show('chatScreen');
loadMessages(c.id);
}

async function loadMessages(id){
try{
const data=await api('/api/conversations/'+id+'/messages');
const box=document.getElementById('messages');

box.innerHTML='';

data.messages.forEach(m=>{
const div=document.createElement('div');
const mine=Number(m.sender_id)===Number(currentUser?.id);
div.className='msg '+(mine?'mine':'theirs');
div.dataset.messageId=m.id;

const text=document.createElement('span');
text.className='message-text';
text.textContent=m.deleted?'Message deleted':m.text;
div.appendChild(text);

if(mine){
const status=document.createElement('span');
status.className='message-status';
status.textContent=m.status==='read'?'✓✓':(m.status==='delivered'?'✓✓':'✓');
div.appendChild(status);
}

box.appendChild(div);
setupMessagePress(div);
});

box.scrollTop=box.scrollHeight;

await api('/api/conversations/'+id+'/read',{method:'POST'});
}catch(e){
console.warn(e.message);
}
}


let messagePressTimer=null;

function hideMessageActions(){
  document.querySelectorAll('.message-actions-backdrop').forEach(x=>x.remove());
  document.querySelectorAll('.message-actions').forEach(x=>x.remove());
}

function showMessageActions(el){
  hideMessageActions();

  if(!el || !el.classList.contains('mine')) return;
  if(el.classList.contains('deleted-message')) return;

  const backdrop=document.createElement('div');
  backdrop.className='message-actions-backdrop';
  backdrop.onclick=hideMessageActions;
  document.body.appendChild(backdrop);

  const actions=document.createElement('div');
  actions.className='message-actions';

  const edit=document.createElement('button');
  edit.textContent='Edit';

  const del=document.createElement('button');
  del.textContent='Delete';
  del.className='delete';

  edit.onclick=async(e)=>{
    e.stopPropagation();
    hideMessageActions();

    const textEl=el.querySelector('.message-text');
    const oldText=textEl ? textEl.textContent : '';

    const updated=prompt('Edit message:',oldText);
    if(updated===null)return;

    const text=updated.trim();
    if(!text || text===oldText)return;

    try{
      await api('/api/messages/'+el.dataset.messageId,{
        method:'PATCH',
        body:JSON.stringify({text})
      });

      await loadMessages(currentConversation.id);
    }catch(e){
      alert(e.message);
    }
  };

  del.onclick=async(e)=>{
    e.stopPropagation();
    hideMessageActions();

    if(!confirm('Delete this message?'))return;

    try{
      await api('/api/messages/'+el.dataset.messageId,{
        method:'DELETE'
      });

      await loadMessages(currentConversation.id);
    }catch(e){
      alert(e.message);
    }
  };

  actions.appendChild(edit);
  actions.appendChild(del);
  el.appendChild(actions);
}

function setupMessagePress(el){
  if(!el || !el.classList.contains('mine'))return;

  el.addEventListener('contextmenu',e=>{
    e.preventDefault();
    e.stopPropagation();
    showMessageActions(el);
  });

  el.addEventListener('touchstart',e=>{
    if(e.touches.length!==1)return;

    clearTimeout(messagePressTimer);

    messagePressTimer=setTimeout(()=>{
      showMessageActions(el);
    },550);
  },{passive:true});

  el.addEventListener('touchend',()=>{
    clearTimeout(messagePressTimer);
  },{passive:true});

  el.addEventListener('touchcancel',()=>{
    clearTimeout(messagePressTimer);
  },{passive:true});

  el.addEventListener('touchmove',()=>{
    clearTimeout(messagePressTimer);
  },{passive:true});
}


async function sendMessage(){
if(!currentConversation)return;

const input=document.getElementById('messageInput');
const text=input.value.trim();

if(!text)return;

input.value='';

try{
await api('/api/conversations/'+currentConversation.id+'/messages',{
method:'POST',
body:JSON.stringify({text})
});

await loadMessages(currentConversation.id);
}catch(e){
alert(e.message);
}
}

let typingTimer=null;

function sendTypingState(typing){
  if(!socket || !currentConversation) return;

  socket.emit('gm:typing', {
    conversationId: currentConversation.id,
    typing: Boolean(typing)
  });
}

function messageTyping(){
  sendTypingState(true);

  clearTimeout(typingTimer);

  typingTimer=setTimeout(()=>{
    sendTypingState(false);
  },1200);
}

function messageKey(e){
  if(e.key==='Enter'&&!e.shiftKey){
    e.preventDefault();
    clearTimeout(typingTimer);
    sendTypingState(false);
    sendMessage();
    return;
  }

  messageTyping();
}


function openUpdates(){
  alert('Updates is ready for the next design stage.');
}

function openCommunities(){
  alert('Communities is ready for the next design stage.');
}

function openCalls(){
  alert('Calls is ready for the next design stage.');
}

function openGemma(){
show('gemmaScreen');
}

function sendGemma(){
const input=document.getElementById('gemmaInput');
const text=input.value.trim();

if(!text)return;

alert('Gemma AI connection can be connected to the AI service next.');
input.value='';
}

function filterChats(){
const q=document.getElementById('search').value.toLowerCase().trim();

document.querySelectorAll('.real-chat').forEach(row=>{
row.style.display=
!q||row.dataset.name.toLowerCase().includes(q)
?'flex':'none';
});
}

function escapeHtml(value){
return String(value)
.replace(/&/g,'&amp;')
.replace(/</g,'&lt;')
.replace(/>/g,'&gt;')
.replace(/"/g,'&quot;')
.replace(/'/g,'&#039;');
}

function logout(){
if(confirm('Log out of G Messenger?')){
localStorage.removeItem('gmessengerToken');
localStorage.removeItem('gmessengerName');
localStorage.removeItem('gmessengerSetup');
token='';
show('loginScreen');
}
}

/* Silent background refresh: no page reload and no visual animation. */
setInterval(()=>{
if(token&&document.getElementById('appScreen').classList.contains('active')){
loadConversations();
}
},5000);

startup();
</script>

</body>
</html>
